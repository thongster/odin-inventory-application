<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/styles.css" />
    <link rel="icon" href="/icon.png" />
    <title>Mage Spellbook | Inventory Application</title>
  </head>
  <body>
    <%- include("partials/header.ejs") %>

    <main class="main">
      <section class="section">
        <header class="section-header section-header-with-action">
          <div class="section-header-text">
            <h2 class="section-title">Mage Spellbook</h2>
            <p class="section-subtitle">
              A catalog of all recorded spells and their arcane properties.
            </p>
          </div>

          <a href="/spells/new" class="button-primary">
            + Add New Spell
          </a>
        </header>

        <% if (spells.length === 0) { %>
          <p class="empty-state">
            No spells have been recorded yet.
          </p>
        <% } else { %>
          <div class="spell-grid">
            <% spells.forEach((spell) => { %>
              <div class="spell-card">
                <header class="spell-card-header">
                  <h3 class="spell-name">
                    <a href="/spells/<%= spell.id %>" class="spell-link">
                      <%= spell.name %>
                    </a>
                  </h3>
                  <span class="spell-element"><%= spell.element %></span>
                </header>

                <p class="spell-description">
                  <%= spell.description %>
                </p>

                <ul class="spell-meta">
                  <li><strong>Damage:</strong> <%= spell.damage %></li>
                  <li><strong>Mana:</strong> <%= spell.mana %></li>
                  <li><strong>Cooldown:</strong> <%= spell.cooldown %>s</li>
                  <li><strong>Range:</strong> <%= spell.range %></li>
                </ul>
              </div>
            <% }) %>
          </div>
        <% } %>
      </section>
    </main>

    <%- include("partials/footer.ejs") %>

  </body>
</html>
