<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/styles.css" />
    <link rel="icon" href="/icon.png" />
    <title>Mage Spellbook | Inventory Application</title>
  </head>
  <body>
    <%- include("partials/header.ejs") %>

    <main class="main">
      <section class="section">
        <article class="spell-detail">
          <!-- Spell Header -->
          <header class="spell-detail-header">
            <h2 class="spell-detail-name"><%= spell.name %></h2>
            <span class="spell-detail-element"><%= spell.element %></span>
          </header>

          <!-- Description -->
          <p class="spell-detail-description"><%= spell.description %></p>

          <!-- Stats -->
          <ul class="spell-detail-stats">
            <li><strong>Damage:</strong> <%= spell.damage %></li>
            <li><strong>Mana Cost:</strong> <%= spell.mana %></li>
            <li><strong>Cooldown:</strong> <%= spell.cooldown %> seconds</li>
            <li><strong>Range:</strong> <%= spell.range %></li>
          </ul>

          <!-- Actions -->
          <div class="spell-detail-actions">
            <a href="/spells/<%= spell.id %>/update" class="button-secondary">
              Update Spell
            </a>

            <form
              action="/spells/<%= spell.id %>/delete"
              method="POST"
              onsubmit="
                return confirm('Are you sure you want to delete this spell?');
              "
            >
              <button type="submit" class="button-danger">Delete Spell</button>
            </form>
          </div>
        </article>
      </section>
    </main>

    <%- include("partials/footer.ejs") %>
  </body>
</html>
