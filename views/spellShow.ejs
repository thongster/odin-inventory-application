<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/styles.css" />
    <link rel="icon" href="/icon.png" />
    <title>Mage Spellbook | Inventory Application</title>
  </head>
  <body>
    <h1>Mage Spellbook</h1>

    <h2><%= spell.name %></h2>
    <p><%= spell.description %></p>
    <p><%= spell.mana %></p>
    <p><%= spell.cooldown %></p>
    <p><%= spell.damage %></p>
    <p><%= spell.range %></p>
    <p><%= spell.element %></p>

    <a href="/spells/<%= spell.id %>/update">Update</a>
    <form
      action="/spells/<%= spell.id %>/delete"
      onsubmit="return confirm('Are you sure you want to delete this spell?');"
      method="POST"
    >
      <button type="submit">Delete</button>
    </form>
  </body>
</html>
